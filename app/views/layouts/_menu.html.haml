.ui.large.inverted.menu#topmenu
  %a.header.item{ href: '/' }
    ZION
    - if !current_user.nil?
      %a.item{ href: events_url, class: active_item('events') }
        %i.ui.calendar.icon
        Eventos
      %a.item{ href: users_url, class: active_item('users') }
        %i.ui.users.icon
        Usuários

    - if !current_user.nil?
      .right.menu
        .ui.dropdown.item
          #profile-avatar
            .ui.avatar.image
              - if(!current_user.account.nil? and !current_user.account.avatar.nil?)
                = image_tag(current_user.account.avatar.image.url(:thumb))
              - else
                = image_tag 'avatar.jpg'
            = current_user.email
            %i.dropdown.icon
          .menu
            %a.item{ href: logout_url }
              %i.sign.out.icon
              Sair
