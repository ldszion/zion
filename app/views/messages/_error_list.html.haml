- if errors.any?
  .ui.error.floating.message
    %i.close.icon
    .header
      %p
        - count = errors.count
        - if count > 1
          #{count} erros ocorreram
        - else
          #{count} erro ocorreu
    %ul.list
      - errors.each do |msg|
        %li= msg
